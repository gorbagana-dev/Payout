repos:
  - repo: https://github.com/doublify/pre-commit-rust
    rev: v1.0
    hooks:
      - id: fmt
        args: ["--", "--check"]
      - id: clippy
        args: ["--workspace", "--all-targets", "--", "-D", "warnings"]
      - id: cargo-check
        args: ["--workspace"]
